title: kazam — beautiful sites from simple YAML
shell: standard

components:
  - type: header
    title: kazam
    eyebrow: YAML → beautiful sites
    subtitle: One Rust binary. No framework, no npm, no runtime JS. Designed so AI agents can author your site end-to-end.

  - type: section
    eyebrow: Why
    heading: Markdown isn't enough. Next.js is too much.
    components:
      - type: markdown
        body: |
          Markdown was designed for humans typing prose. It renders plain pages — grey text on
          white, no cards, no badges, no interactive tables. That was fine when humans wrote
          all the content.

          Now LLMs write the content, and plain markdown wastes what's possible. The usual
          response is to reach for Next.js or a framework-backed docs tool — and inherit a
          thousand-package npm tree, a webpack build, a client runtime, and a supply chain
          you'll never audit.

          kazam is the middle path: **the format stays trivial to generate, but the output is
          genuinely beautiful — and it ships as pure HTML and CSS.** Structured components,
          LLM-friendly schema, themed, composable. No React. No framework. No runtime JS.
          One Rust binary.

  - type: section
    eyebrow: Beyond docs
    heading: And it's not just docs.
    components:
      - type: markdown
        body: |
          Figma for the docs you'd otherwise mock up in Google Docs, the decks you'd otherwise
          build in Gamma, and the SaaS landing pages you'd otherwise wireframe in Figma.
          **Not Figma for free-form visual exploration — that's still Figma.**

      - type: card_grid
        cards:
          - title: Agents can produce it
            description: Hand Claude a brief and get a populated deck, brief, or landing page back — edit-ready.
          - title: Output is the deliverable
            description: Figma exports a PNG or unreliable CSS. kazam output is the production HTML you ship.
          - title: Git-versionable
            description: Every change is a diff in a PR. Figma's version history is a black box by comparison.
          - title: Printable
            description: "`shell: document` and `shell: deck` both print clean. Figma's print story is non-existent."

  - type: section
    eyebrow: 30 seconds
    heading: This YAML…
    components:
      - type: code
        language: yaml
        code: |
          title: Q2 Pipeline Review
          shell: standard

          components:
            - type: header
              title: Q2 Pipeline Review
              subtitle: Three active opportunities

            - type: stat_grid
              columns: 3
              stats:
                - label: Total ACV
                  value: $1.2M
                  color: green
                - label: Weighted
                  value: $480K
                  color: default
                - label: At risk
                  value: $200K
                  color: yellow

      - type: markdown
        body: "…renders to this:"

      - type: header
        title: Q2 Pipeline Review
        subtitle: Three active opportunities

      - type: stat_grid
        columns: 3
        stats:
          - label: Total ACV
            value: $1.2M
            color: green
          - label: Weighted
            value: $480K
            color: default
          - label: At risk
            value: $200K
            color: yellow

  - type: section
    eyebrow: Read more
    heading: Three ways in
    components:
      - type: card_grid
        cards:
          - title: Is it a fit?
            description: The 1,722 → 210 migration story, use cases, and the honest line on what kazam is not.
            links:
              - label: See the fit →
                href: about.html

          - title: How does it work?
            description: The three orthogonal axes, the agent-first authoring model, and why static output has no JS supply chain.
            links:
              - label: Under the hood →
                href: how.html

          - title: Install it
            description: "`cargo install`, `kazam init`, `kazam dev`. You're live on localhost in 60 seconds."
            links:
              - label: Start the guide →
                href: guide.html
